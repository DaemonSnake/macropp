class(A, Object)
{
    /* __POSTFIX__(@BEFORE ; @, end_abstract @) */
    /* __POSTFIX__(@BEFORE ( @, , @) */
    /* __POSTFIX__(@BALENCED ( @, ) @, , @) */
    abstract func() void;

    /* __POSTFIX__(@BEFORE ; @, end_override @) */
    /* __POSTFIX__(@BEFORE ( @, , @) */
    /* __POSTFIX__(@BALENCED ( @, ) @, , @) */
    override func() int {
    }

    /* __POSTFIX__(@BEFORE ; @, end_virtual @) */
    /* __POSTFIX__(@BEFORE ( @, , @) */
    /* __POSTFIX__(@BALENCED ( @, ) @, , @) */
    method func() int {
    }
}
